import React from 'react';
import Questions from './questions';
import Vacancies from './vacancies';

export const QuestionContext = React.createContext(Questions);
export const VacanciesContext = React.createContext(Vacancies);
